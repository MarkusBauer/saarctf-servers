# Cloud init for libvirt test setup

Attaching cloud-init isos to our VMs when booting allows them to auto-magically have 
their networking configured just like they would in a public cloud (hetzner).
